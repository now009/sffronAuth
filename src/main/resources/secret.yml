auth:
  users:
    - username: admin
      password: "1234!"
      roles:
        - ADMIN
        - USER

  # OAuth2 Client 등록 정보
  clients:
    - client-id: saffron-client
      client-secret: "saffron-secret"
      redirect-uri: "http://localhost:8080/login/oauth2/code/saffron"
      scopes:
        - openid
        - profile
        - read
        - write
